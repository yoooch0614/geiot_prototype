# ADR-001: アプリを完成形に近づける設定・保護・フィードバック基盤

- **Status:** Accepted
- **Date:** 2026-07-13
- **Related commit:** `db2386a feat: complete app settings and safety flows`

## Context

このプロトタイプは、絵本を読むコアループは動いていた。一方で、実際に子どもとおうちのひとが使うためには、音・表示・プライバシーの設定、保護者画面の保護、初回案内、保存や読み込みの状態表示が必要だった。

## Decision

### 1. 設定を専用の SETTINGS 画面に集約する

`js/Settings.js` と `modules/Settings/` を追加し、次の設定をブラウザの `localStorage` に保存する。

- BGM
- 効果音
- よみきかせ（開発中・初期状態はオフ）
- 文字サイズ
- アニメーション
- テーマ（昼・自動・夜）
- 画面言語（現在は日本語のみ）
- プライバシー説明

昼夜切り替え用の独立したフローティングボタンは削除し、テーマ設定に統合した。

### 2. よみきかせは段階導入にする

専用音声があるページでは音声ファイルを使い、音声がないページではブラウザの `SpeechSynthesis` を試用フォールバックとして使う。設定画面では「開発中」と表示し、初期状態をオフにする。

### 3. 保護者 PIN を端末内で管理する

初期 PIN はプロトタイプ用の `0000` とする。設定画面から、現在の PIN、新しい PIN、確認用 PIN の順に入力して4桁 PINを変更できる。

PINを3回連続で間違えた場合は30秒間ロックする。PINの入力状態とロック時刻は `ehon.session.v2` に保存し、リロード後もロック状態を維持する。

保護者モードは5分間操作がない場合に自動ロックし、最初のモード選択画面 `MODE` に戻す。クリック、キー入力、タッチ、スクロールを操作として扱う。

### 4. 初回案内と操作フィードバックを追加する

初回の `MODE` 画面で、次の3ステップを案内する。

1. こどもモード
2. おうちのひとモード
3. 写真ミッション

案内は後から再表示できる。保存成功、ミッション完了、PINエラー、保存失敗は画面上のステータスまたはToastで通知する。

### 5. 読み込み・空状態・エラー状態を明示する

- 起動中は `じゅんび中…` を表示する。
- 絵本や思い出がない場合は空状態の説明を表示する。
- 絵本の読み込みに失敗した場合は、失敗したコンテンツを記録し、再試行ボタンを表示する。
- 起動自体に失敗した場合は、サーバー確認と再試行を案内する。

## Consequences

### Positive

- 子ども向け機能と保護者向け機能の使い分けが明確になる。
- 設定が画面ごとに分散せず、変更内容がリロード後も維持される。
- PINの誤入力、保存処理、読み込み失敗でユーザーが状況を判断しやすくなる。
- バックエンドなしでも、同じ端末・同じブラウザ内で設定と利用記録を維持できる。

### Limitations

- PINと設定はブラウザの `localStorage` に保存するため、端末・ブラウザをまたいだ同期はない。
- `localStorage` の値は本番向けの安全な認証基盤ではない。正式版ではサーバー側認証または端末の認証機能を検討する。
- データのエクスポート／一括削除、複数の子どもアカウント、翻訳版は未実装。
- よみきかせのブラウザ音声は端末・ブラウザによって声や品質が変わる。

## Validation

- 全 JavaScript ファイルに `node --check` を実行
- `python tools/validate_content.py` が警告0件
- `git diff --check` を実行
- ローカルサーバーで `index.html`、`app.js`、設定画面などが HTTP 200 で取得できることを確認
