# 編集ガイド（はじめての人向け）

このアプリを編集するための手順書です。プログラミングにあまり慣れていなくても、
「やりたいこと」から場所を探して、例のとおりに書き換えれば変えられるように作ってあります。

---

## 0. さいしょに覚える3つのこと

**① 保存して、ブラウザを再読み込みするだけで反映される**
`python3 serve.py` を動かしたまま、ファイルを編集して保存 → ブラウザで再読み込み（Windows: `Ctrl+R` / Mac: `Cmd+R`）。ビルドやコンパイルは要りません。

**② ファイルは役割で分かれている**

| 役割 | 何が入っているか | ファイル |
|---|---|---|
| 中身（コンテンツ） | 絵本の文章・どの絵を出すか | `content/books/<絵本>.json`（1冊1ファイル） |
| 絵本の並び順 | どの絵本を・どの順で出すか | `content/library.json`（索引） |
| 絵・音 | イラストや音声のファイル | `content/assets/` |
| 画面の構造 | ページに何をどう並べるか（HTML） | `js/screens.js` |
| 見た目 | 色・大きさ・余白（CSS） | `css/style.css` |
| 記録 | 何をどんな形で残すか | `js/Session.js` |

**③ 迷ったら「文章・絵 → `content/books/<絵本>.json`」「見た目 → `style.css`」「並び・演出 → `screens.js`」**
この3つでだいたい足ります。

---

## 1. 編集の準備

1. ターミナルでプロジェクトのフォルダに移動
2. `python3 serve.py` を実行
3. 表示された `http://localhost:8000` をブラウザで開く（iPadは `http://<PCのIP>:8000`）
4. コードエディタ（VS Codeなど）でファイルを開いて編集
5. 保存 → ブラウザ再読み込みで確認

止めるときはターミナルで `Ctrl+C`。

---

## 2. レシピ集（やりたいこと別）

各レシピは「**いま こう → こう書き換える → こうなる**」の順です。
コードの ` ... ` は「省略」の意味なので、そのまま書かないでください。

---

### レシピA：絵本の文章を変える

**ファイル：** `content/books/book-leaf.json`（絵本ごとに1ファイルあります）

その絵本ファイルを開くと、`pages` の中に各ページが並んでいます。
最初のページはこうなっています。

```json
{
  "id": "p1",
  "type": "story",
  "text": "こんにちは！ぼくは かぜの ふぅ。",
  "image": "assets/scene-sky.svg"
}
```

`text` の中身を書き換えます。

```json
  "text": "やっほー！ ぼくは はっぱの はっぴー。",
```

→ 絵本の1ページ目の文章がそのまま変わります。ここは HTML もコードも触りません。

> 💡 `id` は変えないこと（アプリが各ページを見分ける名札なので）。

---

### レシピB：絵本にページを1枚足す

**ファイル：** `content/books/book-leaf.json`（編集したい絵本のファイル）

ページは `pages` という角かっこ `[ ]` の中に、`{ }` を **カンマ区切り** で並べたものです。
足したい場所に `{ }` を1つ追加します。

いま：

```json
"pages": [
  { "id": "p1", "type": "story", "text": "...", "image": "assets/scene-sky.svg" },
  { "id": "p2", "type": "story", "text": "...", "image": "assets/scene-meadow.svg" }
]
```

p1 と p2 の間にページを足す：

```json
"pages": [
  { "id": "p1", "type": "story", "text": "...", "image": "assets/scene-sky.svg" },
  { "id": "p1b", "type": "story", "text": "そらが とっても きれいだね。", "image": "assets/scene-sky.svg" },
  { "id": "p2", "type": "story", "text": "...", "image": "assets/scene-meadow.svg" }
]
```

→ p1 の次に新しいページが増えます。

> ⚠️ **カンマに注意。** `{ }` と `{ }` の間には必ずカンマ `,` が要ります。
> ただし **最後の `{ }` のうしろにはカンマを付けない**。これが一番多いミスです。

---

### レシピC：ミッションを足す

**ファイル：** `content/books/book-leaf.json`（編集したい絵本のファイル）

ミッションは `type` が `"mission"` のページです。お話ページより項目が多めです。

```json
{
  "id": "m3",
  "type": "mission",
  "text": "つぎの たからもの！",
  "prompt": "きいろい はな を さがしてみよう。",
  "doneLabel": "みつけた！",
  "diaryCaption": "きいろい はな、みつけたよ！",
  "developmentDomains": ["色", "自然"],
  "image": "assets/scene-meadow.svg"
}
```

各項目の意味：

- `prompt` … 子どもへのお題（画面の緑の枠に出る）
- `doneLabel` … 撮影ボタンの文字（実際は「📷 みつけた！」と出る）
- `diaryCaption` … 絵本日記に載る一言
- `developmentDomains` … 発達領域のタグ（あとで分析に使う。無くても動く）

→ これを `pages` に足すと、その位置でミッションが始まり、写真を撮って達成できます。

---

### レシピD：新しい絵本を1冊足す（2ステップ）

絵本は **1冊1ファイル** です。足すときは「①ファイルを作る → ②索引に登録する」の2手順。
別々の絵本を担当する人どうしがGitでぶつからないための構成です。

**ステップ①：`content/books/` に新しいファイルを作る**

例として `content/books/book-sora.json` を新規作成し、中身をこう書きます。
（ファイル全体が「1冊の絵本」を表します）

```json
{
  "id": "book-sora",
  "title": "そらの ぼうけん",
  "ageRange": "3-6",
  "cover": "assets/cover-leaf.svg",
  "pages": [
    { "id": "s1", "type": "story", "text": "そらへ しゅっぱつ！", "image": "assets/scene-sky.svg" },
    { "id": "s2", "type": "mission", "text": "みつけよう", "prompt": "しろい くも を さがそう。",
      "doneLabel": "みつけた！", "diaryCaption": "くも、みつけたよ！", "image": "assets/scene-sky.svg" },
    { "id": "send", "type": "end", "text": "たのしかったね！", "image": "assets/scene-sunset.svg" }
  ]
}
```

**ステップ②：`content/library.json`（索引）に、その絵本ファイルを登録する**

いま：

```json
"books": [
  "books/book-leaf.json",
  "books/book-town.json"
]
```

さっき作ったファイルを足す（**リストの最後はカンマなし**に注意）：

```json
"books": [
  "books/book-leaf.json",
  "books/book-town.json",
  "books/book-sora.json"
]
```

→ 絵本選択の画面（SELECT）に3冊目のカードが、この順番で並びます。
`library.json` を触るのは「絵本を足す・消す・並べ替える」ときだけです。日々の文章編集では触りません。

**最低限のルール：**
- `cover`（表紙の絵）が要る
- 最後に `type: "end"` のページを1枚入れる（これが無いと絵本日記が作られません）
- `id` は他の絵本と重複させない
- ①で作ったファイルは、必ず②で索引に登録する（登録しないと画面に出ません）

---

### レシピE：絵を差し替える

**フォルダ：** `content/assets/`

新しい画像（例 `scene-forest.svg` や `.png`）をこのフォルダに入れ、
絵本ファイル（`content/books/<絵本>.json`）の `image` をそのファイル名に変えるだけです。

```json
  "image": "assets/scene-forest.svg"
```

> `assets/` の頭は付けたまま、ファイル名だけ変えます。
> 絵本ファイルが `books/` の中にあっても、パスは **`content/` からの相対**（`assets/...`）で書きます。
> 表紙は `cover`、シールは後述の演出（レシピJ）です。

---

### レシピF：文字や背景の色を変える

**ファイル：** `css/style.css` の一番上（`:root` のところ）

色はここに名前付きでまとまっています。ここを変えると**アプリ全体の色が一括で変わります**。

いま：

```css
:root {
  --green:  #6bbf59;   /* みどり（メイン）*/
  --coral:  #ff7a5c;   /* 撮影ボタンなど */
  --yellow: #ffd23f;   /* おひさま色 */
}
```

例えばメインを青緑にしたいなら：

```css
  --green:  #2fae9b;
```

→ タイトル・「つぎへ」ボタン・進捗表示など、みどり系が一斉に変わります。

`#6bbf59` は「色コード」です。色は Google で「カラーピッカー」と検索すると選べます。

---

### レシピG：本文の文字サイズを変える

**ファイル：** `css/style.css`

本文（絵本の文章）は `.lead` というまとまりです。

```css
.lead {
  font-size: clamp(22px, 6vw, 30px);
  ...
}
```

`clamp(最小, 画面に応じた値, 最大)` という書き方です。もっと大きくしたいなら：

```css
  font-size: clamp(26px, 7vw, 36px);
```

→ 文章が全体的に大きくなります（スマホでも読みやすくなる）。

ミッションのお題枠は `.prompt`、ボタンの文字は `.big-next` / `.mission-shoot` を同じように触ります。

---

### レシピH：ページに新しい要素を足す（少しだけコード）

**ファイル：** `js/screens.js`

「文章の上に小さい見出しを出したい」のように、**構造そのもの**を変えるときはここです。
物語ページの見た目は `STORY` の `render` にあります。

いま：

```js
STORY: {
  render(ctx, { page }) {
    ...
    return `
      <div class="screen reader page--in">
        <button class="back" data-back>‹</button>
        <span class="progress">${ctx.session.pageIndex + 1} / ${count}</span>
        <div class="scene"><img src="${img}" alt="" onerror="this.style.opacity=0"></div>
        <p class="lead">${esc(page.text)}</p>
        <button class="big-next" data-next>つぎへ ›</button>
      </div>`;
  },
```

`<p class="lead">` の**上**に見出しを1行足す：

```js
        <div class="scene"><img src="${img}" alt="" onerror="this.style.opacity=0"></div>
        <p class="page-heading">おはなし</p>
        <p class="lead">${esc(page.text)}</p>
```

→ 絵の下に「おはなし」という見出しが出ます。
見た目（色・大きさ）を整えたければ `css/style.css` に足します：

```css
.page-heading { font-size: 14px; color: #a2ada4; text-align: center; margin: 0; }
```

**ここだけ注意：**
- 全体を囲む `` ` `` （バッククォート）と `< >` の対応を崩さないこと。
- 文章など可変の値を入れるときは `${esc( ... )}` で囲む（`esc` は変な文字が壊れないようにする係）。

---

### レシピI：達成演出の長さを変える

**ファイル：** `js/screens.js` の `ACHIEVE`

達成のシール演出が何秒で物語に戻るかは、この数字（ミリ秒）です。

```js
ACHIEVE: {
  ...
  mount(ctx) {
    setTimeout(() => ctx.advance(), 1600);  // ← 1600 = 1.6秒
  },
```

もっと余韻を長くしたいなら：

```js
    setTimeout(() => ctx.advance(), 2500);  // 2.5秒
```

→ 演出が長く表示されてから物語に戻ります。
絵本完了の間（COMPLETE）も同じやり方で、`COMPLETE` の `mount` の `1500` を変えます。

---

### レシピJ：達成のシール画像・アニメを変える

**シール画像：** `content/assets/sticker-star.svg` を差し替え（同じ名前で上書き）。

**アニメの動き：** `css/style.css` の `@keyframes pop`。

```css
@keyframes pop {
  0%   { transform: scale(.4); opacity: 0; }
  60%  { transform: scale(1.12); }
  100% { transform: scale(1); opacity: 1; }
}
```

`scale(1.12)` の数字を大きくすると、シールがより大きく「ボヨン」と出ます。

---

### レシピK：記録の中身を変える（例：撮影した時刻を残す）

記録は3か所を通ります。1項目足すには、その3か所すべてに足します。

**ファイル：** `js/Session.js` と `js/screens.js`

**① 達成したときに記録する（`Session.js` の `completeMission`）**

いま：

```js
completeMission({ missionId, missionText, caption, photoUrl }) {
  this.runMissions.push({ missionId, missionText, caption, photoUrl });
  ...
}
```

撮影時刻を足す：

```js
completeMission({ missionId, missionText, caption, photoUrl }) {
  this.runMissions.push({ missionId, missionText, caption, photoUrl, createdAt: new Date().toISOString() });
  ...
}
```

**② 絵本日記に含める（`Session.js` の `buildMemory`）**

```js
entries: this.runMissions.map((m) => ({
  photoUrl: m.photoUrl,
  caption: m.caption,
  missionText: m.missionText,
  createdAt: m.createdAt,   // ← 足す
})),
```

**③ 日記に表示する（`js/screens.js` の `DIARY.render`）**

```js
<p class="diary-caption">${esc(e.caption)}</p>
<p class="diary-time">${esc(e.createdAt || "")}</p>   <!-- ← 足す -->
```

→ 絵本日記に、その写真を撮った時刻が出るようになります。

---

### レシピL：絵本日記の一言を変える

**ファイル：** `content/books/<絵本>.json`

日記のキャプションは各ミッションの `diaryCaption` です。

```json
  "diaryCaption": "まるい はっぱ、みつけたよ！",
```

ここを書き換えれば、日記に載る文が変わります。
（`diaryCaption` を書かなかった場合は、お題の文がそのまま使われます。）

---

## 3. よくあるつまずき

**画面が真っ白になった**
→ ファイルを直接ダブルクリックで開いていませんか？ 必ず `python3 serve.py` 経由で開きます。
→ それでも白いときは、ブラウザで `F12`（開発者ツール）を開き、赤いエラー文を見ると原因が分かります。多くは次のカンマ・かっこミスです。

**JSONファイル（`library.json` や `books/` の中）を変えたら動かなくなった**
→ JSON は書式にうるさいです。次を確認：
- `{ }` どうしの間にカンマ `,` があるか
- **最後の要素のうしろにカンマが付いていないか**（余分なカンマはNG）
- 文字は必ず `"ダブルクォート"` で囲む（`'シングル'` はNG）
→ 迷ったら検証コマンド：

```bash
python3 tools/validate_content.py
```

`✓ OK` なら書式は大丈夫です。`✗` の行に場所が出ます。

**画像が出ない（枠だけ表示される）**
→ `image` のファイル名と、`content/assets/` の中の実ファイル名が一致しているか確認。
→ 検証コマンドが「ファイルが見つかりません」と教えてくれます。

**日本語が文字化けする**
→ ファイルの保存形式を **UTF-8** にしてください（VS Codeは既定でUTF-8）。

**編集したのに変わらない**
→ ブラウザの再読み込み（`Ctrl+R` / `Cmd+R`）を忘れていないか。
→ それでも変わらなければ「スーパーリロード」（`Ctrl+Shift+R` / `Cmd+Shift+R`）。

---

## 4. さわらない方がいい場所

次は「つなぎの仕組み」で、見た目や中身とは関係が薄いので、基本は触らなくて大丈夫です。

- `js/ContentRepository.js` … コンテンツを読み込む係
- `js/app.js` … 画面を切り替える係
- `js/Session.js` の `_save` / `_restore` … 保存の仕組み（記録の**中身**を足すレシピKは触ってOK）

もし触って動かなくなったら、配布時の zip から元のファイルに戻せば元通りです。

---

## こまったときの早見表

| やりたいこと | ファイル | 目印 |
|---|---|---|
| 文章を変える | `content/books/<絵本>.json` | `text` |
| ページ・ミッションを足す | `content/books/<絵本>.json` | `pages` |
| 絵本を1冊足す | `content/books/` に新ファイル ＋ `content/library.json` | `books` |
| 絵を差し替える | `content/assets/` | ファイルを置いて `image` を変更 |
| 色を変える | `css/style.css` | 先頭の `:root` |
| 文字の大きさ | `css/style.css` | `.lead` `.prompt` |
| ページの構造を変える | `js/screens.js` | 各画面の `render` |
| 演出の長さ | `js/screens.js` | `setTimeout(... , 数字)` |
| 記録の中身 | `js/Session.js` | `completeMission` `buildMemory` |
| 日記の一言 | `content/books/<絵本>.json` | `diaryCaption` |
